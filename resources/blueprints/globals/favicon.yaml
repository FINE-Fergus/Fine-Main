sections:
  main:
    display: Main
    fields:
      -
        handle: favicon_img
        field:
          mode: grid
          container: images
          restrict: false
          allow_uploads: true
          show_filename: true
          max_files: 1
          display: Favicon
          type: assets
          icon: assets
          listable: hidden
          instructions: 'Upload a square image of at least `512px` by `512px` to automatically generate all standard Favicon sizes.'
          instructions_position: above
          read_only: false
